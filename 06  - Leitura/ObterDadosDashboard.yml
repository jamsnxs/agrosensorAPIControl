info:
  name: ObterDadosDashboard
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/api/leitura-sensores
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Deve retornar a série temporal de dados para plotagem de gráficos no Frontend", function() {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody()).to.be.an('array');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
