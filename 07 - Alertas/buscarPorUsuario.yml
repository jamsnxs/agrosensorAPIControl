info:
  name: buscarPorUsuario
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/api/alertas/usuario/4
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const status = res.getStatus();
        const alertas = res.getBody();

        if (status === 200) {
          test("Deve listar alertas de todas as plantações do usuário", function() {
            expect(alertas).to.be.an('array');
          });
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
