info:
  name: Buscar Vínculo por ID
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/api/plantacoes-sensores/{{id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Busca vínculo plantação-sensor pelo ID"
