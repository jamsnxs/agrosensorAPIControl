info:
  name: deletar
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3000/api/plantacoes/{{id}}
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        // Captura o status HTTP da resposta
        const status = res.getStatus();
        // Aceita remoção (200/204) ou bloqueio por dependências (400)
        test("Deve remover ou barrar remoção caso contenha dependências vinculadas", function() {
          expect(status).to.be.oneOf([200, 204, 400]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
