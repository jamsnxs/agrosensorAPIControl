info:
  name: deletar
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3000/api/plantacoes/1
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const status = res.getStatus();
        test("Deve remover ou barrar remoção caso contenha dependências vinculadas", function() {
          expect(status).to.be.oneOf([200, 204, 400]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
