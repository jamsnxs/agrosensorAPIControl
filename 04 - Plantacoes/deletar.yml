info:
  name: deletar
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3000/api/plantacoes/1
  body:
    type: json
    data: |-
      {
        "nome": "Cultivo de Feijão - Área Norte Modificado",
        "descricao": "Atualização das condições de manejo do solo"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const status = res.getStatus();
        test("Deve remover ou barrar remoção caso contenha dependências vinculadas", function() {
          expect(status).to.be.oneOf([200, 204, 404, 400]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
