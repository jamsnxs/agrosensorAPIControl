info:
  name: Listar Dispositivos
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/api/dispositivos
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        // Confirma que o GET retornou 200 e o corpo é uma lista
        test("Deve listar os dispositivos cadastrados", function() {
          expect(res.getStatus()).to.equal(200);
          expect(res.getBody()).to.be.an('array');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Lista todos os dispositivos cadastrados"
